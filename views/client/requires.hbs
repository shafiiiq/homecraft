<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <title>You are there</title>
</head>

<body>
    {{!-- hello --}}
    <form class="form" action="/client/requires" method="post">
        <div class="container">
            <div class="left">
                <a href="/index.html">
                    <i class="fa-solid fa-arrow-left"></i>
                </a>
                <img src="/images/logo.white.png" alt="">
                <div class="material">
                    <h1>
                        Material Breakdowns
                    </h1>
                    <div class="said">
                        <div class="items">
                            <p>Bricks</p>
                            <select name="bricks" id="bricks">
                            </select>
                        </div>
                        <div class="items">
                            <p>Cement</p>
                            <select name="cement" id="cement" class="blue">
                                {{!-- ///// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Sand</p>
                            <select name="sand" id="sand">
                                {{!-- ////// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Wood</p>
                            <select name="wood" id="wood">
                                {{!-- ///// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Wires</p>
                            <select name="wires" id="wires">
                                {{!-- ///// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Pipes</p>
                            <select name="pipes" id="pipes">
                                {{!-- ////// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Switch Board</p>
                            <select name="switchcBoard" id="switchcBoard">
                                {{!-- ///// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>DB Board</p>
                            <select name="dbBoard" id="dbBoard">
                                {{!-- ////// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Paint</p>
                            <select name="paint" id="paint">
                                {{!-- ///// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Switches</p>
                            <select name="switches" id="switches">
                                {{!-- ///// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Tiles</p>
                            <select name="tiles" id="tiles">
                                {{!-- ///// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Sink</p>
                            <select name="sink" id="sink">
                                {{!-- ////// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Washbasin</p>
                            <select name="washbasin" id="washbasin">
                                {{!-- ////// --}}
                            </select>
                        </div>
                        <div class="items">
                            <p>Taps</p>
                            <select name="taps" id="taps">
                            </select>
                        </div>
                        <div class="items">
                            <p>Shower</p>
                            <select name="shower" id="shower">
                            </select>
                        </div>
                        <div class="items">
                            <p>Closet</p>
                            <select name="closet" id="closet">
                            </select>
                        </div>
                        <div class="items">
                            <p>Roof</p>
                            <select name="roof" id="roof">
                            </select>
                        </div>
                        <div class="items">
                            <p>Light</p>
                            <select name="light" id="light">
                            </select>
                        </div>
                        <div class="items">
                            <p>Fan</p>
                            <select name="fan" id="fan">
                            </select>
                        </div>
                        <div class="items">
                            <p>Interlock</p>
                            <select name="interlock" id="interlock">
                            </select>
                        </div>
                    </div>
                    <p>
                        Building our house involves using different materials like bricks, concrete, wood, and drywall
                        to create a comfortable <br>and <span>sturdy home.</span>
                    </p>
                </div>
            </div>
            <div class="right">
                <h1>Requirements</h1>
                <div class="client">
                    <div class="volume">
                        <input name="measureLand" class="placeholder" type="text"
                            placeholder="Measurment of land (sq.ft)">
                        <input name="budget" class="placeholder" type="text" placeholder="Budget">
                        <input name="noFloors" class="placeholder" type="text" placeholder="No. of floors">
                    </div>
                    <div class="dates">
                        <div class="width flex">
                            <p class="label">Start Date</p>
                            <input type="text" name="startDate" class="one" id="startDateInput">
                            <span class="material-symbols-rounded calender-symbol" id="date">
                                calendar_month
                            </span>
                            <input type="date" class="one absol" id="startDateValue" style="opacity: 0;">
                        </div>
                        <div class="width flex">
                            <p class="label">End Date Expected</p>
                            <input type="date" name="endDate" class="two">
                        </div>
                    </div>
                    <textarea class="placeholder" name="additional" id="" cols="30" rows="10"
                        placeholder="Additional requirements (require 1, require2, .....)"></textarea>
                    <textarea class="placeholder" name="currentAddress" id="currentAdrress" cols="30" rows="10"
                        placeholder="Current address"></textarea>
                    <textarea class="placeholder" name="siteAddress" id="siteAddress" cols="30" rows="10"
                        placeholder="Construction Site address"></textarea>
                    <div class="requirements">
                        <div class="items">
                            <select name="noCarporch" id="noCarporch">
                                <option selected>No.of Carporch</option>
                                <option value="0">0</option>
                                <option value="1">1</option>
                                <option value="2">2</option>
                                <option value="3">3</option>
                                <option value="4">4</option>
                                <option value="5">5</option>
                                <option value="6">6</option>
                                <option value="7">7</option>
                            </select>
                        </div>
                        <div class="items">
                            <select name="noLiving" id="noLiving">
                                <option selected>No.of Living Room</option>
                                <option value="0">0</option>
                                <option value="1">1</option>
                                <option value="2">2</option>
                                <option value="3">3</option>
                                <option value="4">4</option>
                                <option value="5">5</option>
                                <option value="6">6</option>
                                <option value="7">7</option>
                            </select>
                        </div>
                        <div class="items">
                            <select name="noDining" id="noDining">
                                <option selected>No.of Dining Hall</option>
                                <option value="0">0</option>
                                <option value="1">1</option>
                                <option value="2">2</option>
                                <option value="3">3</option>
                                <option value="4">4</option>
                                <option value="5">5</option>
                                <option value="6">6</option>
                                <option value="7">7</option>
                            </select>
                        </div>
                        <div class="items">
                            <select name="noBedroom" id="noBedroom">
                                <option selected>No.of Bedrooms</option>
                                <option value="0">0</option>
                                <option value="1">1</option>
                                <option value="2">2</option>
                                <option value="3">3</option>
                                <option value="4">4</option>
                                <option value="5">5</option>
                                <option value="6">6</option>
                                <option value="7">7</option>
                            </select>
                        </div>
                        <div class="items">
                            <select name="noKitchen" id="noKitchen">
                                <option selected>No.of Kitchens</option>
                                <option value="0">0</option>
                                <option value="1">1</option>
                                <option value="2">2</option>
                                <option value="3">3</option>
                                <option value="4">4</option>
                                <option value="5">5</option>
                                <option value="6">6</option>
                                <option value="7">7</option>
                            </select>
                        </div>
                        <div class="items">
                            <select name="noBathroom" id="noBathroom">
                                <option selected>No.of Bathrooms</option>
                                <option value="0">0</option>
                                <option value="1">1</option>
                                <option value="2">2</option>
                                <option value="3">3</option>
                                <option value="4">4</option>
                                <option value="5">5</option>
                                <option value="6">6</option>
                                <option value="7">7</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <input class="absolute" type="submit" value="Submit">
    </form>

    <script src="https://kit.fontawesome.com/f83293ade7.js" crossorigin="anonymous"></script>

    <script>

        // Fetch bricks data and populate the dropdown
        fetch('/json/bricks.json')
            .then(response => response.json())
            .then(data => {
                const bricksSelect = document.getElementById('bricks');

                data.forEach(type => {
                    const option = document.createElement('option');
                    option.textContent = type.type;
                    option.value = type.type;
                    bricksSelect.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching bricks data:', error));



        // cement push
        fetch('/json/cement.json')
            .then(response => response.json())
            .then(data => {
                // Get the select element
                const selectElement = document.getElementById('cement');

                // Remove the 'Loading...' option
                selectElement.innerHTML = '';

                // Add options for each company
                data.cement_companies.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company; // You can set value if needed
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching data:', error));
        //cement push ends

        // Sand fetch
        fetch('/json/sand.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('sand');
                selectElement.innerHTML = '';

                data.sand_types.forEach(sand => {
                    const option = document.createElement('option');
                    option.textContent = sand.type;
                    option.value = sand.type;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching sand data:', error));


        // Wood fetch
        fetch('/json/wood.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('wood');
                selectElement.innerHTML = '';

                data.wood_types.forEach(wood => {
                    const option = document.createElement('option');
                    option.textContent = wood.type;
                    option.value = wood.type;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching wood data:', error));

        // Wire fetch
        fetch('/json/wires.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('wires');
                selectElement.innerHTML = '';

                data.wires_companies.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching wire companies data:', error));


        // Pipe fetch
        fetch('/json/pipes.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('pipes');
                selectElement.innerHTML = '';

                data.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching pipe companies data:', error));



        // Switchboard fetch
        fetch('/json/switch-board.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('switchcBoard');
                selectElement.innerHTML = '';

                data.forEach(type => {
                    const option = document.createElement('option');
                    option.textContent = type.type;
                    option.value = type.type;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching switchboard types data:', error));



        // Distribution Board fetch
        fetch('/json/db-board.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('dbBoard');
                selectElement.innerHTML = '';

                data.db_board_companies.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching distribution board companies data:', error));

        // Paint fetch
        fetch('/json/paint.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('paint');
                selectElement.innerHTML = '';

                data.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching paint companies data:', error));

        // Switch fetch
        fetch('/json/switches.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('switches');
                selectElement.innerHTML = '';

                data.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching switch companies data:', error));

        // Tiles fetch
        fetch('/json/tiles.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('tiles');
                selectElement.innerHTML = '';

                data.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching tile companies data:', error));


        // Sink fetch
        fetch('/json/sink.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('sink');
                selectElement.innerHTML = '';

                data.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching sink companies data:', error));


        // Washbasin fetch
        fetch('/json/washbasin.json')
            .then(response => response.json())
            .then(data => {
                const selectElement = document.getElementById('washbasin');
                selectElement.innerHTML = '';

                data.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    selectElement.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching washbasin companies data:', error));


        // Fetch taps data and populate the dropdown
        fetch('/json/taps.json')
            .then(response => response.json())
            .then(data => {
                const tapsSelect = document.getElementById('taps');

                data.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    tapsSelect.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching taps data:', error));


        // Fetch shower data and populate the dropdown
        fetch('/json/shower.json')
            .then(response => response.json())
            .then(data => {
                const showerSelect = document.getElementById('shower');

                data.forEach(type => {
                    const option = document.createElement('option');
                    option.textContent = type.type;
                    option.value = type.type;
                    showerSelect.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching shower data:', error));


        // Fetch closet data and populate the dropdown
        fetch('/json/closet.json')
            .then(response => response.json())
            .then(data => {
                const closetSelect = document.getElementById('closet');

                data.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    closetSelect.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching closet data:', error));



        // Fetch roof data and populate the dropdown
        fetch('/json/roof.json')
            .then(response => response.json())
            .then(data => {
                const roofSelect = document.getElementById('roof');

                data.forEach(type => {
                    const option = document.createElement('option');
                    option.textContent = type.type;
                    option.value = type.type;
                    roofSelect.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching roof data:', error));


        // Fetch light data and populate the dropdown
        fetch('/json/light.json')
            .then(response => response.json())
            .then(data => {
                const lightSelect = document.getElementById('light');

                data.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    lightSelect.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching light data:', error));


        // Fetch fan data and populate the dropdown
        fetch('/json/fan.json')
            .then(response => response.json())
            .then(data => {
                const fanSelect = document.getElementById('fan');

                data.forEach(company => {
                    const option = document.createElement('option');
                    option.textContent = company.company;
                    option.value = company.company;
                    fanSelect.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching fan data:', error));


        // Fetch interlock data and populate the dropdown
        fetch('/json/interlock.json')
            .then(response => response.json())
            .then(data => {
                const interlockSelect = document.getElementById('interlock');

                data.forEach(type => {
                    const option = document.createElement('option');
                    option.textContent = type.type;
                    option.value = type.type;
                    interlockSelect.appendChild(option);
                });
            })
            .catch(error => console.error('Error fetching interlock data:', error));


        document.addEventListener("DOMContentLoaded", function () {
            // Get current date
            var currentDate = new Date();

            // Format the current date to MM/DD/YYYY format
            var month = (currentDate.getMonth() + 1).toString().padStart(2, '0');
            var day = currentDate.getDate().toString().padStart(2, '0');
            var year = currentDate.getFullYear();
            var formattedDate = month + '/' + day + '/' + year;

            // Set default value to the text input field
            document.getElementById("startDateInput").value = formattedDate;

            // Listen for changes in the date input
            document.getElementById("startDateValue").addEventListener("change", function () {
                // Get the selected date
                var selectedDate = new Date(this.value);
                var selectedMonth = (selectedDate.getMonth() + 1).toString().padStart(2, '0');
                var selectedDay = selectedDate.getDate().toString().padStart(2, '0');
                var selectedYear = selectedDate.getFullYear();
                var formattedSelectedDate = selectedMonth + '/' + selectedDay + '/' + selectedYear;

                // Set the selected date to the text input field
                document.getElementById("startDateInput").value = formattedSelectedDate;
            });
        });

        //helloo
    </script>
</body>

</html>